import os # Thư viện này được import nhưng không bao giờ dùng

def my_function( a,b ): # Có khoảng trắng thừa trước dấu phẩy
    c=a+b # Thiếu khoảng trắng quanh dấu = và +
    return c

print(my_function(1,2))
